# =============================================================================
# TEMPLATE DE VARIABLES D'ENVIRONNEMENT - AGENDA FUNNEL
# =============================================================================
# Copiez ce fichier vers .env.local et remplissez les valeurs appropriées

# Variables publiques Supabase (exposées côté client)
# Ces variables sont sécurisées et peuvent être exposées côté client
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Variables privées Supabase (côté serveur uniquement)
# Cette clé donne accès admin - utilisée uniquement dans les API routes
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Variables optionnelles pour Vercel
VERCEL_URL=
NEXT_PUBLIC_VERCEL_URL=

# =============================================================================
# INSTRUCTIONS POUR LE DÉPLOIEMENT SUR VERCEL
# =============================================================================
# 1. Dans le dashboard Vercel, allez dans Settings > Environment Variables
# 2. Ajoutez TOUTES ces variables avec les vraies valeurs :
#    - NEXT_PUBLIC_SUPABASE_URL
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY  
#    - SUPABASE_SERVICE_ROLE_KEY (SANS le préfixe NEXT_PUBLIC_)
# 3. Redéployez votre application après avoir ajouté les variables
# 4. IMPORTANT: Seules les variables avec NEXT_PUBLIC_ sont exposées côté client
